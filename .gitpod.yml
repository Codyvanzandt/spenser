tasks:
  - init: pip3 install -r requirements.txt
    command: |
      git config core.hooksPath hooks
      find ./hooks -type f -exec chmod +x {} \;
      export PYTHONDONTWRITEBYTECODE=1
      alias run_tests="cd /workspace/spenser && python -m pytest -s -v"
  
github:
  prebuilds:
    master: true
